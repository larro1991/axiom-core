[package]
name = "axiom-clock"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Hybrid logical clock for AXIOM protocol"

[features]
default = ["std"]
std = ["axiom-types/std"]

[dependencies]
axiom-types = { workspace = true }
