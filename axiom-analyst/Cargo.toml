[package]
name = "axiom-analyst"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "AXIOM Analyst - Security event correlation and attack chain detection"

[features]
default = ["std"]
std = ["axiom-types/std", "axiom-guardian/std", "axiom-watcher/std"]

[dependencies]
axiom-types = { workspace = true }
axiom-guardian = { workspace = true }
axiom-watcher = { workspace = true }
hashbrown = { workspace = true }
thiserror = { workspace = true }
